/*
Page showing the available gestures for which recordings can be made and the current amount of recordings for each gesture
*/
import { memo } from "react";





export const RecordingPage = memo(props => {

    // TODO show a list of gestures each with the recording number and recording percentage

    // Provide a modal that allows for actual recordings to take place
    return <></>
});
